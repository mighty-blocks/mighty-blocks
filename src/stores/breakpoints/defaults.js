export const DESKTOP = { slug: 'desktop', name: 'Desktop' };
export const TABLET = { slug: 'tablet', name: 'Tablet', size: '1024px' };
export const MOBILE = { slug: 'mobile', name: 'Mobile', size: '767px' };

export const ITEMS_DEFAULT = [
	DESKTOP,
	TABLET,
	MOBILE,
];

export const CURRENT_DEFAULT = DESKTOP.slug;
